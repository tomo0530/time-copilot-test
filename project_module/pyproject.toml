[project]
name = "project-module"
version = "0.1.0"
description = ""
authors = []
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "kaggle>=1.8.3",
    "pydantic>=2.10.6,<3.0.0",
    "python-dotenv>=1.2.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "mypy~=1.15.0",
    "pytest~=8.4.1",
    "pytest-cov~=6.2.1",
    "pytest-mock~=3.12.0",
    "ruff~=0.11.2",
]
